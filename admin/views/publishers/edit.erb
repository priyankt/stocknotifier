<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:publishers, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:publishers, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:publishers, :edit, :id => @publisher.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :publisher, url(:publishers, :update, :id => @publisher.id), :method => :put, :class => 'form-horizontal', :multipart => true do |f| %>
    <%= partial 'publishers/form', :locals => { :f => f } %>
  <% end %>
</div>
